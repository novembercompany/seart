{
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.3"
  },
  "name": "seart",
  "scripts": {
    "build": "lerna exec --scope @seart/styles -- tsc",
    "start": "cd packages/app && yarn start",
    "dev": "cd packages/app && yarn dev"
  }
}
